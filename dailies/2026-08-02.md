# Daily — 2026-08-02 — Domingo

## Agenda
- Nenhum evento agendado


## Tarefas
- Nenhuma tarefa


## Estado
- Energia: 🟢🟡🔴
- Humor: 

## Atividades com Hermes

### Migração do Hermes para o Mac Mini (TUI)
- Sessão longa (57 msgs) planejando a mudança: desktop app recomendada sobre terminal puro, porque traz gateway integrado e tools de browser/computer-use nativas.
- Descoberta no meio do caminho: o bundle de skills/scripts/perfis já existe em `~/hermes-state/` versionado no GitHub (`ramoneees/hermes-backup`), mas o remote **nunca recebeu push** — `git push` retornou 404.
- Sem `gh` CLI, sem chave SSH e sem credencial HTTPS no sandbox, fechamos o plano em **rsync/scp one-shot** do Linux pro Mac + env vars manuais; Mac vai ficar sempre ligado e logado (auto-launch via Login Items).

### Configuração da máquina e migração (Telegram)
- Ramon perguntou a config atual — `olympus`, Ubuntu 24.04.4 LTS, 12 vCPUs / 62 GiB RAM, `~/.hermes/` em 4.3 GB — e abriu o plano de mover tudo pra um Mac Mini M4 16 GB.
- Mapa de superfície montado: `state.db` (410 MB) é a memória de longo prazo; serviços systemd ativos (gateway, dashboard, crisp); vault do Obsidian e dois remotes (Gitea funcional, GitHub só local).
- Mais tarde, na mesma sessão, confirmou: existe sim **`hermes backup`** oficial (`--quick` para snapshot crítico: config, `state.db`, `.env`, `auth`, skills) — vai ser usado pra levar estado de um host pro outro.

## Notas
### Aprendizados
- 

### Bloqueios
- 

## Reflexão
> 
